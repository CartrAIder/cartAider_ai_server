[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "cartgate"
version = "0.1.0"
description = "AI exit-gate for self-checkout: receipt-conditioned anomaly detection"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["cartgate"]
